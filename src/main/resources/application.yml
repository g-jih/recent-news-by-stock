spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/stockdb?serverTimezone=UTC&characterEncoding=UTF-8
    username: stockuser
    password: stock0818
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org.hibernate.SQL: debug
    #org.hibernate.type: trace

server:
  port: 8080

external:
  api:
    id: PcqeBZmnsgUIVu02TLDJ
    password: oacFmqOCBg